# gb-523 数据中心热约束机柜布局

请生成英文短名为 `datacenter-thermal-capacity-planner` 的 Go 全栈 Web 项目。它面向数据中心容量规划团队，维护房间热区、机柜和设备负载，在离线场景中评估功率、散热与邻接热影响并给出候选布局。系统只做规划决策支持，不连接 BMS、空调、PDU 或机柜控制器，不自动下发任何设备动作。不得实现资产库存、采购、工单、订单、财务或泛化监控大屏。

## 项目主要需求

### 1. 核心实体

- `ThermalZone`：至少含 `id`、`zone_code`、`name`、`cooling_capacity_kw`、`supply_temp_c`、`max_return_temp_c`、`adjacency_json`、`zone_status`；贯穿数据库及前后端全部业务层。
- `Rack`：至少含 `id`、`zone_id`、`rack_code`、`row_index`、`column_index`、`power_limit_kw`、`airflow_limit_cfm`、`rack_status`、`version`；位置唯一且关联热区，贯穿全栈。
- `EquipmentLoad`：至少含 `id`、`name`、`power_kw`、`heat_kw`、`airflow_cfm`、`rack_units`、`redundancy_group`、`preferred_zone_id`、`load_status`；表示待规划负载，不做库存管理，贯穿全栈。
- `LayoutScenario`：至少含 `id`、`name`、`scenario_status`、`rack_assignments_json`、`total_power_kw`、`peak_temp_c`、`constraint_violations_json`、`score`、`version`、`created_by`、`approved_by`；保存完整输入快照和评估结果，贯穿全栈。

### 2. 核心页面

- `/zones` 热区容量页：消费 `ThermalZone`、`Rack`，编辑容量边界并显示区域利用率。
- `/racks` 机柜布局页：消费 `Rack`、`EquipmentLoad`，用稳定网格展示占用、功率和气流约束。
- `/loads` 设备负载页：消费 `EquipmentLoad`、`ThermalZone`，维护规划输入并批量校验。
- `/planner` 场景规划页：消费 `LayoutScenario`、`Rack`、`EquipmentLoad`，生成候选布局并解释评分与违规项。
- `/audit` 审计与版本页：消费 `LayoutScenario`、`ThermalZone`，比较版本、审批方案并检索审计事件。

### 3. 差异算法与状态流

- 约束校验至少覆盖机柜功率、气流、U 位、热区总冷量、区域回风温度、冗余组隔离和不可用机柜。
- 实现确定性的候选布局算法：先按约束紧度排序负载，再对候选机柜计算容量余量、邻接热惩罚、热点惩罚和偏好奖励；相同输入必须得到相同结果，并返回无法放置的具体约束证据。
- 实现简化热传播模型：机柜热量按热区邻接权重影响相邻区域，输出每区估算回风温度、余量和假设。不得伪装成 CFD 或真实设备测温。
- `LayoutScenario` 状态为 `draft -> evaluating -> pending_review -> approved -> archived`；批准前必须无 `critical` 违规，审批与结果写入同一事务，使用版本号防止并发覆盖。

### 4. 横切关注点

- JWT/RBAC 角色 `planner`、`reviewer`、`admin`，贯穿用户角色表、后端中间件/路由、前端守卫/按钮和类型。
- 审计记录容量边界、机柜位置、负载输入、算法参数、场景状态和批准操作，保留 request ID 与前后摘要。
- 全局错误处理统一输入错误、容量冲突、乐观锁冲突和无权限响应；前端不得静默降级为成功。
- 访问日志、请求追踪、本地限流和 panic recovery 独立实现，日志不得泄露 JWT 或数据库密码。

### 5. 共享枚举、组件、hooks 与中间件

- `RackStatus = available | reserved | unavailable | maintenance`：后端 `backend/internal/constants/rack.go`，前端 `frontend/src/types/rack.ts`。
- `ScenarioStatus = draft | evaluating | pending_review | approved | archived`：后端 `backend/internal/constants/scenario.go`，前端 `frontend/src/types/scenario.ts`。
- README 列明枚举在数据库、model/dto/service/handler、前端 store/component/page 的全部位置。
- 共享组件：`CapacityMeter`（热区/机柜页）、`ConstraintBadge`（规划/审计页）、`VersionComparePanel`（规划/审计页）。
- hooks：`useAuth`、`useScenarioEvaluation`，后者负责提交评估与轮询，禁止页面重复实现。
- 中间件至少独立实现 `RequestID`、`AccessLog`、`Auth`、`RBAC`、`Recovery`、`RateLimit`。

### 6. 技术与规模红线

- 前端 Angular 17 + TypeScript + Angular CLI/esbuild + Angular Material + RxJS，使用独立 services/store 和路由守卫。
- 后端 Go 1.22 + Gin + GORM + PostgreSQL，validator/v10、结构化日志、构造器注入、事务、`%w`、优雅停机。
- 目标 2500-4200 行功能 Go 代码、24-42 个功能 `.go` 文件且低于 5000 行；不计测试、vendor、生成代码与前端。严禁空壳拆文件或单文件聚合职责。
- `/api/v1`、`/healthz`、`/readyz`、统一响应/分页/错误码齐全；规划算法、热传播和状态机必须有表驱动测试。
- 禁止 TODO、placeholder、静态假数据冒充 API、handler 直连数据库和非事务多步写入。

### 7. 文件结构强制清单

```text
./
├── go.work                 # use ./backend
├── docker-compose.yml
├── .env
├── .env.example
├── .gitignore
├── README.md
├── runtime_smoke.json
├── backend/
│   ├── go.mod
│   ├── Dockerfile
│   ├── cmd/server/main.go
│   └── internal/{config,model,dto,repository,service,handler,router,middleware,constants,planner,util}/
├── frontend/
│   ├── Dockerfile
│   ├── nginx.conf
│   └── src/app/{api,stores,types,components/common,hooks,pages,router,utils}/
└── output/execution.md
```

每个实体必须分开 model/dto/repository/service/handler/router 文件；约束评估、候选排序、热传播和评分分别成包内文件。严禁合并职责到单一文件。

`runtime_smoke.json` 只能是启动 manifest，构建/API/Browser 证据只能进入 `output/execution.md`。提交以下可解析内容；后端须支持 smoke 时的 GORM SQLite 与 Compose 时的 PostgreSQL：

```json
{
  "mode": "service",
  "start": "go run ./cmd/server",
  "workdir": "backend",
  "env": {
    "PORT": "20523",
    "DB_DRIVER": "sqlite",
    "DB_DSN": "file:runtime-smoke?mode=memory&cache=shared",
    "DB_AUTO_MIGRATE": "true",
    "JWT_SECRET": "runtime-smoke-gb523-jwt-secret-at-least-32-bytes"
  },
  "ready_url": "http://127.0.0.1:20523/healthz",
  "ready_status": [200],
  "startup_timeout": 30
}
```

### 8. 部署、README 与端口

- Compose 项目名 `datacenter-thermal-capacity-planner`；前端 `18523`、后端 `19523`、PostgreSQL `57523`，容器端口 `80/8080/5432`。
- Compose 顶层 `name` 且不写 `version:`，容器名用 `${COMPOSE_PROJECT_NAME:-datacenter-thermal-capacity-planner}` 前缀；数据库使用命名卷，数据库、后端和前端均配置 healthcheck 并按健康状态依赖，`docker compose ps` 中所有长驻服务最终必须为 healthy。
- `.env` 和示例包含 Compose 名、三端口、数据库变量、JWT、CORS、算法迭代上限和日志级别。Nginx `/api` 反代 `backend:8080`，支持 Angular 路由 fallback；中文目录一键启动。
- README 中文且 Docker 启动置顶，包含复制 env、地址、测试账号、功能、技术栈、目录、变量、API、枚举位置、模型假设、安全边界、本地开发、常见问题和 License。

### 9. 强制真实验收

1. 根目录真实运行 `go work sync`、`go build ./backend/...`、`go vet ./backend/...`、`go test ./backend/...`；规模实测必须为 2500-4200 行、24-42 个功能 `.go` 文件且低于 5000 行；前端执行依赖安装、类型检查/测试和 `npm run build` 生产构建。
2. 真实运行 Compose 配置校验、构建启动和状态检查，数据库/后端必须 healthy，前端与 `/healthz`、`/readyz` 可访问。
3. 至少 8 个接口冒烟，覆盖登录、热区创建、机柜创建、负载创建、场景创建、评估、查看违规、提交复核、批准、版本冲突和审计查询；把真实状态码、断言和时间只写入 `output/execution.md`，不得写入 `runtime_smoke.json`。
4. 只用 Codex 内置 Browser 验证登录、5 页面、网格布局、候选生成、违规解释和审批交互，禁止外部 Chrome；截图文件一律保存到 `output/`，Browser 步骤与结论只写入 `output/execution.md`。
5. 执行 `docker compose down -v --remove-orphans` 并确认本项目 Compose 状态为空，不影响其他项目。
6. 项目目录独立执行 `git init`，设置仓库本地身份 `git config user.name "blueship581"`、`git config user.email "brysj.hhrhl.g@gmail.com"`。先提交除 `output/` 验证证据外的实现文件并取得实现 commit hash，将 hash 写入 `output/execution.md`，再把 `output/execution.md` 与截图作为第二次报告提交；最终 `git status --short` 为空。
